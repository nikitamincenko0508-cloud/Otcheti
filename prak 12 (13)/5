numbers = list(map(int, input().split()))

counts = {}

for num in numbers:
    counts[num] = counts.get(num, 0) + 1

pairs = 0
for k in counts.values():
    if k > 1:
        pairs += k * (k - 1) // 2

print(pairs)
